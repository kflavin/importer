---
loader:
  dev:
    enabled: false
    weekly:
      rate: cron(0 15 ? * * *)
    monthly:
      rate: cron(0 16 ? * * *)
  master:
    enabled: true
    weekly:
      rate: cron(0 2 ? * * *)
    monthly:
      rate: cron(0 3 * * ? *)
  prod:
    enabled: true
    weekly:
      rate: rate(9999 days)
      # rate: cron(0 7 6-27 * ? *)
    monthly:
      rate: rate(9999 days)
      # rate: cron(0 9 6-27 * ? *)
downloader:
  dev:
    enabled: false
    rate: cron(0 17,23 ? * * *)
  master:
    enabled: true
    rate: cron(0 17,23 ? * * *)
  prod:
    enabled: true
    rate: cron(0 17,23 ? * * *)
