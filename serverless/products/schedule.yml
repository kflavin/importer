---
edge:
  enabled: false
  rate: rate(9999 days)
rc:
  enabled: false
  rate: cron(0 14 ? * * *)
staging:
  enabled: true
  rate: cron(0 1 ? * * *)
prod:
  enabled: true
  rate: cron(0 6 6-27 * ? *)